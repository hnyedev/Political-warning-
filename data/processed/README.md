# `data/processed/` — structured personas

**Generated by:** Phase 1 attribute extraction (Ollama)

| File | In git? |
|---|---|
| `personas_structured.jsonl` | No — gitignored |

One JSON object per line (`PersonaProfile`). Required before `run_pipeline.py`.
