# `data/raw/` — downloaded dataset

**Generated by:** `python -m pipeline.p01_persona_extraction.download_dataset`

| File | Size (approx.) | In git? |
|---|---|---|
| `personas_sample_10k.parquet` | ~5–50 MB | No — gitignored |

Replicate: run the download command after `pip install -r requirements.txt`.
