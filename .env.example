# Copy to .env and fill in (never commit .env):
#   cp .env.example .env

# HuggingFace — optional, if latam-gpt/personas requires auth
HF_TOKEN=

# Ollama — optional overrides (defaults work if Ollama runs locally)
# OLLAMA_HOST=http://127.0.0.1:11434
# OLLAMA_MODEL=llama3.2
